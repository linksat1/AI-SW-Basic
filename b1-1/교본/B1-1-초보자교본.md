# 시스템 관제 자동화 스크립트 개발 — 초보자 완전 교본

> **이 교본은?**
> 리눅스를 처음 접하는 분도 처음부터 끝까지 따라할 수 있도록,
> **모든 명령어에 '무엇을 하는지'와 '왜 하는지'** 를 함께 설명합니다.
> 순서대로 따라가면 평가까지 완성할 수 있습니다.

---

## 전체 흐름 한눈에 보기

```
[0단계] 개념 이해
    ↓
[1단계] 실습 환경 만들기 (OrbStack + Ubuntu 22.04)
    ↓
[2단계] SSH 보안 설정 (포트 변경 + Root 차단)
    ↓
[3단계] 방화벽(UFW) 설정
    ↓
[4단계] 계정과 그룹 만들기
    ↓
[5단계] 디렉토리 구조와 권한 설정
    ↓
[6단계] 앱 실행 환경 구성 (환경변수 + 키 파일)
    ↓
[7단계] Python 앱 실행 확인 (Boot Sequence)
    ↓
[8단계] monitor.sh 스크립트 작성
    ↓
[9단계] crontab 자동 실행 등록
    ↓
[10단계] 보너스 스크립트 (report.sh / log_archive.sh)
    ↓
[평가 준비] 항목별 설명 방법
```

---

## 0단계: 시작 전 핵심 개념 이해

이 미션에서 자주 나오는 개념을 미리 이해하고 시작하면 명령어가 훨씬 쉽게 보입니다.

### 0.1 리눅스가 왜 서버에 쓰이나요?

리눅스는 무료이고, 안정적이고, 인터넷 서버의 95% 이상이 리눅스로 운영됩니다.
개발자가 만든 프로그램은 결국 리눅스 서버 위에서 돌아가기 때문에, 리눅스를 모르면 개발한 것을 배포조차 할 수 없습니다.

### 0.2 리눅스 권한의 3요소

리눅스의 모든 파일에는 **누가 접근할 수 있는지** 권한이 붙어 있습니다.

```
소유자(owner)  : 파일을 만든 사람 (혼자만의 권한)
그룹(group)    : 팀처럼 묶인 사람들 (같은 그룹이면 공유)
기타(other)    : 그 외 모든 사람
```

권한의 종류:

```
r (read)    = 4 : 읽기 가능
w (write)   = 2 : 쓰기 가능
x (execute) = 1 : 실행 가능
```

숫자 예시:

```
750 = 소유자(7=rwx) 그룹(5=r-x) 기타(0=---)
```

→ 소유자는 모든 것 가능, 그룹은 읽고 실행만, 기타는 아무것도 못 함

### 0.3 sudo가 뭔가요?

`sudo`는 "관리자(root) 권한으로 실행해줘"라는 뜻입니다.
서버 설정 파일을 바꾸거나 시스템 명령을 실행할 때 필요합니다.

```bash
sudo 명령어   # 이 명령어를 관리자 권한으로 실행
```

### 0.4 셸(Shell)이 뭔가요?

셸은 우리가 타이핑하는 명령어를 운영체제에 전달해주는 프로그램입니다.
이 미션에서 사용하는 셸은 `bash`입니다.

### 0.5 리다이렉션 기호 `>` vs `>>`

로그 기록에서 가장 중요한 개념입니다.

```bash
명령어 >  파일   # 덮어쓰기: 기존 내용이 사라지고 새로 작성
명령어 >> 파일   # 이어쓰기: 기존 내용 뒤에 줄을 추가
```

예시:

```bash
echo "첫 번째" >  test.log   # test.log: "첫 번째"
echo "두 번째" >  test.log   # test.log: "두 번째" ← 첫 번째가 사라짐!
echo "세 번째" >> test.log   # test.log: "두 번째\n세 번째" ← 누적!
```

**로그는 반드시 `>>`를 써야 합니다.** `>`를 쓰면 매번 덮어써져서 로그가 사라집니다.

---

## 1단계: 실습 환경 만들기

### 1.1 OrbStack 설치 확인

OrbStack은 Mac에서 리눅스를 가상으로 실행해주는 프로그램입니다.
실제 서버처럼 동작하는 Ubuntu 환경을 5분 안에 만들 수 있습니다.

```bash
# Mac 터미널에서 실행: OrbStack이 설치되어 있는지 확인
orb version
# 정상이면 버전 번호가 출력됩니다
# 설치가 안 되어 있다면 https://orbstack.dev 에서 다운로드
```

### 1.2 Ubuntu 22.04 머신 생성

```bash
# Ubuntu 22.04 LTS 리눅스 머신을 'b1-lab'이라는 이름으로 생성
# 이 명령 하나로 이미지 다운로드 → 설치 → 실행까지 자동으로 됩니다
orb create ubuntu:22.04 b1-lab
```

**왜 Ubuntu 22.04인가?**
Ubuntu는 가장 많이 쓰이는 서버 리눅스입니다. 22.04 LTS는 2027년까지 장기 지원(Long-Term Support)이 보장되어 안정적입니다.

### 1.3 Ubuntu 머신 접속

```bash
# 생성한 b1-lab 머신에 셸로 접속
orb shell b1-lab
# 성공하면 프롬프트가 아래처럼 바뀝니다:
# ubuntu@b1-lab:~$
```

### 1.4 기본 확인 및 패키지 설치

접속 후 Ubuntu 안에서 아래 명령을 실행합니다.

```bash
# OS 버전 확인 — Ubuntu 22.04인지 확인
cat /etc/os-release
# NAME="Ubuntu", VERSION="22.04.x LTS" 가 나오면 정상

# 현재 로그인한 사용자 확인
whoami
# 출력: ubuntu

# 관리자(root) 권한 사용 가능 여부 확인
# 이 명령이 오류 없이 'root'를 출력하면 sudo가 잘 됩니다
sudo whoami
# 출력: root

# 패키지 목록을 최신으로 갱신
# apt는 Ubuntu의 앱 설치 관리자입니다 (Windows의 앱스토어 같은 것)
sudo apt update

# 필수 도구 설치
# acl     : 파일 권한을 더 세밀하게 설정하는 도구
# ufw     : 방화벽 설정 도구 (Uncomplicated Firewall)
# net-tools: 네트워크 상태를 확인하는 도구 모음
# unzip   : zip 파일 압축 해제 도구
# -y      : 설치 중 "계속할까요?" 질문에 자동으로 yes 답변
sudo apt install -y acl ufw net-tools unzip
```

### 1.5 실습 파일 머신으로 복사

agent-app.zip 파일을 Mac에서 Ubuntu 머신으로 복사합니다.

```bash
# Mac 터미널에서 실행 (orb shell 밖에서!)
# /path/to/는 실제 agent-app.zip이 있는 위치로 바꾸세요
# 예: ~/Downloads/agent-app.zip 이라면 아래처럼 실행
# 예: orb push b1-lab ~/Downloads/agent-app.zip /tmp/agent-app.zip

cspag5955@b1-lab-test:~$ cp /Users/cspag5955/Downloads/agent-app.zip .

# 또는 macOS 공유 폴더 경로를 이용하는 방법:
# OrbStack 머신 내부에서 /mac/Users/사용자명/ 으로 Mac 파일에 접근 가능
# 예: cp /mac/Users/나의계정/Downloads/agent-app.zip /tmp/
```

```bash
# Ubuntu 머신 안에서 파일이 잘 복사됐는지 확인
# ls /tmp/agent-app.zip

ls
# 파일이 있으면 정상

# zip 압축을 풀기 전에 압축된 파일 목록 보기
# 그전에 zip, unzip화일 설치 필요

unzip -l agent-app.zip

Archive:  agent-app.zip
  Length      Date    Time    Name
---------  ---------- -----   ----
  6498144  2026-05-20 11:11   agent-app-linux-x86
      354  2026-05-20 11:11   __MACOSX/._agent-app-linux-x86
  7537848  2026-05-18 17:06   agent-app-linux-arm64
      219  2026-05-18 17:06   __MACOSX/._agent-app-linux-arm64
---------                     -------
 14036565                     4 files

```

---

## 2단계: SSH 보안 설정

### 왜 SSH 포트를 바꾸고 Root를 차단해야 하나요?

**SSH(Secure Shell)** 는 원격으로 서버에 접속하는 방법입니다.
기본 포트는 **22번**인데, 전 세계 해커들의 자동 공격 프로그램(봇)이 22번 포트만 집중 공격합니다.


SSH 서버 설치 확인 및 설치

```
# 설치 여부 확인
dpkg -l | grep openssh-server

# 설치되어 있지 않으면 설치
sudo apt update && sudo apt install -y openssh-server
```


SSH 서비스 시작 및 자동 시작 등록

```
# 서비스 시작
sudo systemctl start ssh

# 부팅 시 자동 시작 등록
sudo systemctl enable ssh

# 상태 확인
sudo systemctl status ssh
```



```
[해커 봇의 자동 공격 흐름]
인터넷의 모든 IP → 22번 포트 시도 → root 로그인 시도 → 비밀번호 무차별 대입
```

포트를 **20022**로 바꾸면:
→ 자동 공격 봇 대부분이 22번만 시도하므로 우리 서버는 찾지 못함 (공격 노이즈 99% 감소)

Root 원격 접속을 차단하면:
→ 비밀번호가 유출되더라도 root로 직접 들어올 수 없음 (피해 최소화)

### 2.1 SSH 설정 파일 열기

```bash
# SSH 서버의 설정 파일을 nano 편집기로 열기
# /etc/ssh/sshd_config 는 SSH 서버의 모든 동작을 결정하는 설정 파일
sudo nano /etc/ssh/sshd_config
```

**nano 편집기 사용법:**

- 화살표 키: 커서 이동
- `Ctrl + W`: 텍스트 검색 (Port 라고 입력 후 Enter)
- `Ctrl + O` → Enter: 저장
- `Ctrl + X`: 종료

파일 안에서 아래 내용을 찾아 수정합니다:

```
# 수정 전 (주석 처리된 상태, # 이 붙어 있음)
#Port 22
#PermitRootLogin prohibit-password

# 수정 후 (# 제거하고 값 변경)
Port 20022
PermitRootLogin no
```

- `Port 20022` : SSH 접속 포트를 22 → 20022로 변경
- `PermitRootLogin no` : root 계정으로 원격 로그인 완전 차단

### 2.2 SSH 서비스 재시작

설정 파일을 수정했으면 SSH 서비스를 재시작해야 적용됩니다.
(설정 파일을 저장한다고 바로 적용되지 않습니다 — 서비스를 재시작해야 반영됩니다)

```bash
# SSH 서비스(데몬)를 재시작하여 변경된 설정 적용
# systemctl은 서비스를 시작/중지/재시작하는 명령어
sudo systemctl restart sshd
```

### 2.3 설정 확인

```bash
# 설정 파일에서 Port와 PermitRootLogin 줄만 추출하여 확인
# grep -E : 정규식으로 검색 (|는 OR 조건)
# ^ : 줄의 시작을 의미 (주석 처리된 #Port는 제외됨)
sudo grep -E "^Port|^PermitRootLogin" /etc/ssh/sshd_config
# 출력: Port 20022 / PermitRootLogin no

# 실제로 20022 포트로 SSH가 듣고(LISTEN) 있는지 네트워크 소켓 상태 확인
# ss : 소켓 상태 확인 도구 (netstat의 현대적 대체품)
# -t : TCP 소켓만 표시
# -u : UDP 소켓만 표시 (여기서는 TCP SSH가 대상)
# -l : LISTEN 상태인 것만 표시
# -n : 포트 번호를 숫자로 표시 (서비스 이름 대신)
# -p : 어떤 프로세스가 쓰고 있는지 표시
sudo ss -tulnp | grep sshd
# 출력: tcp LISTEN ... 0.0.0.0:20022 ... ("sshd",...)
```

**성공 기준:** `20022` 포트가 LISTEN 상태로 표시되면 완료입니다.

> **주의:** 이후 SSH로 접속할 때는 포트 번호를 명시해야 합니다.
>
> ```bash
> ssh -p 20022 사용자명@서버IP
> ```

---

## 3단계: 방화벽(UFW) 설정

### 방화벽이 왜 필요한가요?

방화벽은 서버의 **문지기**입니다.
설정한 포트로 들어오는 접속만 허용하고, 나머지는 모두 차단합니다.

```
[방화벽 없을 때]
인터넷 → 어떤 포트로든 접속 가능 (위험!)

[방화벽 있을 때]
인터넷 → 20022(SSH)만 통과  ✅
         15034(앱)만 통과   ✅
         그 외 모든 포트 차단 ❌
```

### 3.1 UFW 규칙 설정 및 활성화

```bash
# SSH 포트 20022 허용
# 반드시 UFW 활성화(enable) 전에 허용 규칙을 먼저 추가해야 합니다!
# 나중에 추가하면 SSH 접속이 끊겨버립니다
sudo ufw allow 20022/tcp

# 앱 포트 15034 허용
# 우리가 실행할 Python 앱이 이 포트를 사용합니다
sudo ufw allow 15034/tcp

# UFW 활성화
# 이 명령을 실행하면 방화벽이 켜집니다
# "Command may disrupt existing ssh connections. Proceed with operation (y|n)?"
# → y 를 입력하면 됩니다
sudo ufw enable

# 방화벽 상태 확인
# Status: active 이면 켜진 것, 허용된 포트 목록이 보여야 합니다
sudo ufw status
```

**정상 출력 예시:**

```
Status: active
Default: deny (incoming)    ← 기본: 외부 접속 모두 차단

To                         Action      From
--                         ------      ----
20022/tcp                  ALLOW IN    Anywhere   ← SSH 허용
15034/tcp                  ALLOW IN    Anywhere   ← 앱 허용
```

> **Tip:** `Default: deny (incoming)` 은 "외부에서 들어오는 모든 접속을 기본으로 차단" 한다는 뜻입니다.
> 허용 목록에 있는 포트만 통과할 수 있습니다.

---

## 4단계: 계정과 그룹 만들기

### 왜 역할별 계정이 필요한가요?

실제 회사의 서버에는 여러 사람이 접속합니다.모두 같은 계정을 쓰면:

- 누가 어떤 파일을 수정했는지 알 수 없음
- 한 명의 실수가 전체 시스템에 영향을 줌
- 테스터가 중요한 설정 파일을 실수로 삭제 가능

**역할에 따라 계정과 그룹을 분리하는 것** 이 서버 운영의 기본입니다.

| 계정        | 역할        | 특징                |
| ----------- | ----------- | ------------------- |
| agent-admin | 운영/관리자 | cron 실행, 앱 구동  |
| agent-dev   | 개발자      | 스크립트 작성, 배포 |
| agent-test  | QA/테스터   | 테스트만 수행       |

| 그룹         | 포함 계정        | 접근 가능한 것           |
| ------------ | ---------------- | ------------------------ |
| agent-common | admin, dev, test | 공용 업무 파일           |
| agent-core   | admin, dev       | 중요 파일 (API 키, 로그) |

### 4.1 그룹 먼저 생성

```bash
# agent-common 그룹 생성
# groupadd : 그룹을 만드는 명령어
# 그룹을 먼저 만들어야 계정을 만들 때 그룹에 넣을 수 있습니다
sudo groupadd agent-common

# agent-core 그룹 생성
sudo groupadd agent-core

# 그룹이 잘 만들어졌는지 확인
# /etc/group : 모든 그룹 정보가 저장된 파일
# grep "agent-" : "agent-"가 포함된 줄만 출력
grep "agent-" /etc/group
# 출력: agent-common:x:XXXX:  /  agent-core:x:XXXX:
```

### 4.2 계정 생성

```bash
# agent-admin 계정 생성
# useradd : 새 사용자 계정을 만드는 명령어
# -m : 홈 디렉토리 자동 생성 (/home/agent-admin/ 폴더가 생깁니다)
# -s /bin/bash : 이 계정이 사용할 셸을 bash로 지정
sudo useradd -m -s /bin/bash agent-admin

# agent-dev 계정 생성
sudo useradd -m -s /bin/bash agent-dev

# agent-test 계정 생성
sudo useradd -m -s /bin/bash agent-test
```

### 4.3 비밀번호 설정

```bash
# agent-admin 비밀번호 설정
# passwd : 비밀번호를 설정하거나 변경하는 명령어
# 실행 후 새 비밀번호를 두 번 입력합니다
sudo passwd agent-admin

# agent-dev 비밀번호 설정
sudo passwd agent-dev

# agent-test 비밀번호 설정
sudo passwd agent-test
```

### 4.4 계정을 그룹에 추가

```bash
# agent-common 그룹에 3개 계정 모두 추가
# usermod : 계정 정보를 수정하는 명령어
# -a : append (추가) - 기존 그룹을 유지하면서 새 그룹 추가
# -G : 보조 그룹 지정
# 주의: -a 없이 -G만 쓰면 기존 그룹이 모두 제거됩니다! 반드시 -aG 함께 사용
sudo usermod -aG agent-common agent-admin
sudo usermod -aG agent-common agent-dev
sudo usermod -aG agent-common agent-test

# agent-core 그룹에는 admin, dev만 추가 (test는 제외 - 최소 권한 원칙)
sudo usermod -aG agent-core agent-admin
sudo usermod -aG agent-core agent-dev
```

### 4.5 sudoers 설정 (agent-admin에게 sudo 권한 부여)

```bash
# agent-admin이 sudo를 비밀번호 없이 실행할 수 있도록 설정
# crontab으로 자동 실행할 때 비밀번호 입력이 불가능하기 때문입니다
# visudo : sudoers 파일을 안전하게 편집하는 명령어
# (일반 텍스트 편집기로 sudoers를 수정하면 문법 오류 시 sudo 자체가 깨집니다)
sudo visudo

# 파일 맨 아래에 아래 줄을 추가
agent-admin ALL=(ALL) NOPASSWD:ALL
# 의미: agent-admin은 모든 서버에서 모든 명령을 비밀번호 없이 sudo 실행 가능
```

### 4.6 설정 확인

```bash
# 각 계정의 그룹 소속 확인
# id : 계정의 UID(사용자번호), GID(그룹번호), 소속 그룹 목록 출력
id agent-admin
# 출력 예: uid=1000(agent-admin) gid=...(agent-admin) groups=...,agent-common,agent-core

id agent-dev
# groups에 agent-common, agent-core 모두 포함되어야 합니다

id agent-test
# groups에 agent-common만 포함되어야 합니다 (agent-core 없음)
```

---

## 5단계: 디렉토리 구조와 권한 설정

### 5.1 디렉토리 생성

```bash
# agent-admin 홈 디렉토리 아래에 앱 관련 폴더 구조 생성
# mkdir -p : 중간 경로가 없어도 한 번에 생성 (p = parents)
sudo mkdir -p /home/agent-admin/agent-app          # 앱 루트 디렉토리
sudo mkdir -p /home/agent-admin/agent-app/upload_files  # 공용 파일 업로드 폴더
sudo mkdir -p /home/agent-admin/agent-app/api_keys # API 키 저장 폴더 (보안 중요)
sudo mkdir -p /home/agent-admin/agent-app/bin      # 스크립트 저장 폴더

# 로그 디렉토리 생성 (/var/log 는 시스템 로그가 모이는 표준 위치)
sudo mkdir -p /var/log/agent-app
```

### 5.2 소유자와 그룹 설정

```bash
# agent-app 전체의 소유자를 agent-admin으로 변경
# chown : change owner (소유자 변경)
# -R : recursive (하위 모든 파일/폴더에 재귀 적용)
# agent-admin:agent-admin = 소유자:그룹
sudo chown -R agent-admin:agent-admin /home/agent-admin/agent-app

# upload_files: 공용 폴더이므로 그룹을 agent-common으로 변경
# → agent-common 소속(admin, dev, test) 모두 접근 가능
sudo chown agent-admin:agent-common /home/agent-admin/agent-app/upload_files

# api_keys: 중요 보안 파일이므로 그룹을 agent-core로 변경
# → agent-core 소속(admin, dev)만 접근 가능 (test는 접근 불가)
sudo chown agent-admin:agent-core /home/agent-admin/agent-app/api_keys

# 로그 디렉토리: 중요 운영 정보이므로 그룹을 agent-core로 변경
sudo chown agent-admin:agent-core /var/log/agent-app
```

### 5.3 권한(Permission) 숫자 설정

```bash
# upload_files: 소유자와 그룹 모두 읽기/쓰기/실행 가능, 외부 차단
# 2775 에서 맨 앞 2는 setgid 비트 = 새로 만들어지는 파일이 폴더의 그룹을 자동 상속
#   7 = rwx (소유자: 모든 권한)
#   7 = rwx (그룹: 모든 권한)
#   5 = r-x (기타: 읽기+실행만, 실제로는 0이 더 안전하지만 디렉토리 탐색용)
sudo chmod 2775 /home/agent-admin/agent-app/upload_files

# api_keys: 소유자만 완전 권한, 그룹은 읽기+실행만, 외부 완전 차단
#   7 = rwx (소유자: 모든 권한)
#   5 = r-x (그룹: 읽기+실행만)
#   0 = --- (기타: 아무 권한 없음)
sudo chmod 750 /home/agent-admin/agent-app/api_keys

# /var/log/agent-app: 소유자와 그룹 모두 읽기/쓰기/실행 가능, 외부 완전 차단
#   7 = rwx (소유자: 모든 권한)
#   7 = rwx (그룹: 모든 권한 — 로그 쓰기를 위해 w 필요)
#   0 = --- (기타: 아무 권한 없음)
sudo chmod 770 /var/log/agent-app
```

### 5.4 ACL로 세밀한 권한 추가 (선택적 강화)

ACL(Access Control List)은 기본 권한보다 더 세밀하게 "특정 그룹에게 특정 권한"을 줄 수 있습니다.

```bash
# upload_files에 agent-common 그룹 읽기/쓰기/실행 권한 추가
# setfacl : ACL 설정 명령어
# -m : modify (수정)
# g:agent-common:rwx = 그룹 agent-common에게 rwx 권한 부여
sudo setfacl -m g:agent-common:rwx /home/agent-admin/agent-app/upload_files

# -d : default (기본값) = 이 폴더 안에 새로 만들어지는 파일에도 자동 적용
sudo setfacl -d -m g:agent-common:rwx /home/agent-admin/agent-app/upload_files

# api_keys에 agent-core 그룹만 접근 가능하도록 ACL 설정
sudo setfacl -m g:agent-core:rwx /home/agent-admin/agent-app/api_keys
sudo setfacl -d -m g:agent-core:rwx /home/agent-admin/agent-app/api_keys

# /var/log/agent-app에 agent-core 그룹 접근 허용
sudo setfacl -m g:agent-core:rwx /var/log/agent-app
sudo setfacl -d -m g:agent-core:rwx /var/log/agent-app
```

### 5.5 권한 확인

```bash
# ls -la : 파일 목록을 권한과 함께 자세히 표시
# -l : 자세한 형식 (권한, 소유자, 크기, 날짜 등)
# -a : 숨김 파일(.으로 시작하는 파일)도 표시
sudo ls -la /home/agent-admin/agent-app/

# getfacl : ACL 권한 상세 확인
# 기본 권한으로는 볼 수 없는 ACL 설정을 확인할 때 사용
sudo getfacl /home/agent-admin/agent-app/upload_files
sudo getfacl /home/agent-admin/agent-app/api_keys
sudo getfacl /var/log/agent-app
```

**getfacl 출력 읽는 법:**

```
# file: var/log/agent-app      ← 파일 경로
# owner: agent-admin           ← 소유자
# group: agent-core            ← 그룹
user::rwx                      ← 소유자(agent-admin) 권한
group::rwx                     ← 기본 그룹(agent-core) 권한
group:agent-core:rwx           ← agent-core 그룹 ACL 권한
mask::rwx                      ← 실제 최대 허용 권한
other::---                     ← 기타 사용자 권한 (없음)
```

---

## 6단계: 앱 실행 환경 구성

### 6.1 agent-admin 계정으로 전환

```bash
# su : switch user (사용자 전환)
# - : 전환하면서 해당 계정의 환경변수와 홈 디렉토리도 함께 적용
su - agent-admin
# 비밀번호 입력 후 프롬프트가 agent-admin@b1-lab:~$ 로 바뀝니다
```

### 6.2 앱 파일 압축 해제

```bash
# 앱 디렉토리로 이동
cd /home/agent-admin/agent-app

# /tmp에 복사해둔 agent-app.zip 압축 해제
# unzip : zip 파일 압축 해제
# -d . : 현재 디렉토리에 압축 해제
unzip /tmp/agent-app.zip -d .

# 압축 해제된 파일 확인
ls -la
# agent-app-linux-arm64 또는 agent-app-linux-x86 파일이 있어야 합니다

# 실행 권한 부여 (zip으로 복사하면 실행 권한이 없을 수 있습니다)
chmod +x agent-app-linux-arm64   # ARM CPU (M1/M2 Mac용)
# 또는
chmod +x agent-app-linux-x86     # x86 CPU (Intel Mac / 일반 PC)
```

### 6.3 API 키 파일 생성

```bash
# api_keys 디렉토리에 키 파일 생성
# echo "내용" > 파일 : 내용을 파일로 저장
echo "agent_api_key_test" > /home/agent-admin/agent-app/api_keys/t_secret.key

# 키 파일 권한을 소유자만 읽기 가능하도록 설정 (600 = rw-------)
# 이렇게 하면 다른 사람이 API 키를 볼 수 없습니다
chmod 600 /home/agent-admin/agent-app/api_keys/t_secret.key

# 파일 내용 확인
cat /home/agent-admin/agent-app/api_keys/t_secret.key
# 출력: agent_api_key_test
```

### 6.4 환경 변수 설정

환경 변수는 프로그램이 실행될 때 참조하는 설정값입니다.
`AGENT_HOME=/home/agent-admin/agent-app`을 설정하면 이후 `$AGENT_HOME`으로 긴 경로를 짧게 쓸 수 있습니다.

```bash
# agent-admin의 .bashrc 파일에 환경 변수 추가
# .bashrc : 이 계정으로 터미널을 열 때마다 자동으로 실행되는 설정 파일
# cat >> 파일 << 'EOF' ... EOF : 여러 줄을 한 번에 파일에 추가하는 방법
cat >> /home/agent-admin/.bashrc << 'EOF'

# Agent 앱 환경 변수
export AGENT_HOME=/home/agent-admin/agent-app           # 앱 루트 경로
export AGENT_PORT=15034                                  # 앱이 사용할 포트
export AGENT_UPLOAD_DIR=$AGENT_HOME/upload_files        # 파일 업로드 경로
export AGENT_KEY_PATH=$AGENT_HOME/api_keys/t_secret.key # API 키 파일 경로
export AGENT_LOG_DIR=/var/log/agent-app                  # 로그 저장 경로
EOF

# 변경사항을 현재 터미널에 즉시 적용
# source : 파일을 읽어 현재 셸에 실행 (다시 로그인하지 않아도 됨)
source /home/agent-admin/.bashrc

# 환경 변수가 잘 설정됐는지 확인
# echo $변수명 : 환경 변수 값 출력
echo $AGENT_HOME
echo $AGENT_PORT
echo $AGENT_KEY_PATH
```

**정상 출력:**

```
/home/agent-admin/agent-app
15034
/home/agent-admin/agent-app/api_keys/t_secret.key
```

---

## 7단계: Python 앱 실행 확인 (Boot Sequence)

### 7.1 앱 실행

```bash
# agent-admin 계정으로 전환 (아직 아니라면)
su - agent-admin

# 앱 디렉토리로 이동
cd /home/agent-admin/agent-app

# 앱 실행 (ARM CPU용 - M1/M2 Mac 기준)
./agent-app-linux-arm64
# 또는 Intel CPU용
# ./agent-app-linux-x86
```

### 7.2 Boot Sequence 5단계 확인

모든 단계가 `[OK]`로 나오고 마지막에 **"Agent READY"** 가 나와야 합니다.

```
>>> Starting Agent Boot Sequence...
[1/5] Checking User Account               [OK]
   ... Running as service user 'agent-admin' (uid=1000)
[2/5] Verifying Environment Variables     [OK]
   ... All required Envs correct
[3/5] Checking Required Files             [OK]
   ... Verified 'secret.key' with correct key string.
[4/5] Checking Port Availability          [OK]
   ... Port 15034 is available.
[5/5] Verifying Log Permission            [OK]
   ... Log directory is writable: /var/log/agent-app
------------------------------------------------------------
All Boot Checks Passed!
Agent READY
```

**각 단계가 [FAIL]이면 확인할 것:**

| 단계 | [FAIL] 원인                           | 해결 방법                                                |
| ---- | ------------------------------------- | -------------------------------------------------------- |
| 1/5  | agent-admin이 아닌 다른 계정으로 실행 | `su - agent-admin` 후 실행                             |
| 2/5  | 환경 변수가 없음                      | `source ~/.bashrc` 실행                                |
| 3/5  | 키 파일 경로나 내용 오류              | `cat $AGENT_KEY_PATH` 로 내용 확인                     |
| 4/5  | 15034 포트가 이미 사용 중             | `ss -tulnp \| grep 15034` 로 확인 후 해당 프로세스 종료 |
| 5/5  | `/var/log/agent-app` 권한 오류      | `sudo chmod 770 /var/log/agent-app`                    |

### 7.3 앱이 실행 중인지 확인 (다른 터미널에서)

앱을 실행한 채로 새 터미널을 열어서 확인합니다.

```bash
# 15034 포트가 LISTEN 상태인지 확인
# 앱이 정상 실행되면 0.0.0.0:15034 로 접속 대기 상태가 됩니다
ss -tlnp | grep 15034
# 출력: tcp LISTEN 0 ... 0.0.0.0:15034 ...
```

> **앱 종료:** `Ctrl + C` 를 누르면 앱이 종료됩니다.

---

## 8단계: monitor.sh 스크립트 작성

### monitor.sh가 무엇인가요?

매분마다 자동으로 실행되어:

1. 앱이 살아있는지 확인 (Health Check)
2. 방화벽이 켜져 있는지 확인
3. CPU/메모리/디스크 사용률 수집
4. 임계값 초과 시 경고 출력
5. 결과를 로그 파일에 기록

서버에 문제가 생기면 로그를 보고 "언제, 무슨 문제가 있었는지" 추적할 수 있습니다.

### 8.1 스크립트 파일 생성

```bash
# root 또는 sudo 권한으로 스크립트 파일 생성
# 먼저 bin 디렉토리 확인
sudo ls /home/agent-admin/agent-app/bin/

# nano로 스크립트 파일 생성 및 편집
sudo nano /home/agent-admin/agent-app/bin/monitor.sh
```

아래 내용을 그대로 붙여넣기 합니다:

```bash
#!/bin/bash
# =============================================================
# monitor.sh - 시스템 관제 자동화 스크립트
# 소유자: agent-dev  그룹: agent-core  권한: 750
# 실행자: agent-admin (agent-core 그룹 소속이므로 실행 가능)
# =============================================================

# ---- 설정값 (여기서 감시 대상을 변경할 수 있습니다) ----
APP_PROCESS="agent-app-linux-arm64"        # 감시할 프로세스 이름
APP_PORT=15034                              # 감시할 포트 번호
LOG_FILE="/var/log/agent-app/monitor.log"  # 로그를 저장할 파일 경로
MAX_LOG_SIZE=$((10 * 1024 * 1024))         # 최대 로그 크기: 10MB (bytes 단위)
MAX_LOG_FILES=10                            # 보관할 최대 로그 파일 개수

# ---- 임계값 (이 값을 넘으면 [WARNING] 출력) ----
CPU_THRESHOLD=20    # CPU 사용률 경고 기준: 20%
MEM_THRESHOLD=10    # 메모리 사용률 경고 기준: 10%
DISK_THRESHOLD=80   # 디스크 사용률 경고 기준: 80%

# =============================================================
# 로그 로테이션 함수 (로그 파일이 10MB를 넘으면 자동 분리)
# =============================================================
rotate_log() {
    # 로그 파일이 없으면 아무것도 하지 않고 함수 종료
    if [ ! -f "$LOG_FILE" ]; then
        return
    fi

    # stat -c%s : 파일 크기를 바이트 단위로 가져옴
    local size
    size=$(stat -c%s "$LOG_FILE" 2>/dev/null || echo 0)

    # 파일 크기가 MAX_LOG_SIZE(10MB) 이상이면 로테이션 시작
    if [ "$size" -ge "$MAX_LOG_SIZE" ]; then
        # 뒤에서부터 번호를 올립니다: .9 삭제 → .8을 .9로 → ... → .1을 .2로
        # seq 9 -1 1 : 9, 8, 7, ..., 1 순서로 숫자 생성
        for i in $(seq $((MAX_LOG_FILES - 1)) -1 1); do
            [ -f "${LOG_FILE}.$i" ] && mv "${LOG_FILE}.$i" "${LOG_FILE}.$((i + 1))"
        done
        # 현재 로그를 .1로 이동
        mv "$LOG_FILE" "${LOG_FILE}.1"
        # 새 빈 로그 파일 생성
        touch "$LOG_FILE"
    fi
}

# ---- 현재 시각 기록 ----
# date "+형식" : 날짜/시간을 지정한 형식으로 출력
TIMESTAMP=$(date "+%Y-%m-%d %H:%M:%S")

echo ""
echo "====== SYSTEM MONITOR RESULT ======"
echo ""

# =============================================================
# [1] HEALTH CHECK — 프로세스와 포트가 정상인지 확인
# 비정상이면 exit 1로 즉시 종료 (cron에 실패 신호를 보냄)
# =============================================================
echo "[HEALTH CHECK]"

# 프로세스 확인
# ps -eo pid,args : 모든 프로세스의 PID와 전체 명령 경로를 출력
# grep "$APP_PROCESS" : 앱 프로세스명이 포함된 줄 필터
# grep -v grep : "grep 명령 자체"가 검색 결과에 잡히는 것을 제외
# grep -v "bash -c" : 쉘 래퍼 프로세스 제외
# awk '{print $1}' : 첫 번째 컬럼(PID)만 추출
# head -1 : 여러 개가 나오면 첫 번째만 사용
PID=$(ps -eo pid,args | grep "$APP_PROCESS" | grep -v grep | grep -v "bash -c" | awk '{print $1}' | head -1)

if [ -z "$PID" ]; then
    # -z : 변수가 비어있으면 (empty)
    echo "Checking process '$APP_PROCESS'... [FAIL] Process not running!"
    exit 1   # 프로세스 없음 = 서비스 중단 → 즉시 종료
else
    echo "Checking process '$APP_PROCESS'... [OK] (PID: $PID)"
fi

# 포트 확인
# ss -tulnp | grep ":포트번호 " : 해당 포트가 LISTEN 상태인지 확인
PORT_STATUS=$(ss -tulnp 2>/dev/null | grep ":${APP_PORT} ")

if [ -z "$PORT_STATUS" ]; then
    echo "Checking port $APP_PORT... [FAIL] Port not listening!"
    exit 1   # 포트 없음 = 서비스 불가 → 즉시 종료
else
    echo "Checking port $APP_PORT... [OK]"
fi

echo ""

# =============================================================
# [2] 방화벽 상태 점검 — 비활성이면 경고만 (종료하지 않음)
# 이유: 방화벽이 꺼져 있어도 서비스 자체는 살아있기 때문
# =============================================================
echo "[FIREWALL CHECK]"

# ufw status 명령으로 활성 상태 확인
# 2>/dev/null : 오류 메시지는 버림 (컨테이너 환경에서 오류가 날 수 있음)
UFW_STATUS_FILE="/var/run/ufw-status"

if ufw status 2>/dev/null | grep -q "Status: active"; then
    echo "Firewall (UFW)... [OK] Active"
elif [ -f "$UFW_STATUS_FILE" ] && grep -q "^active" "$UFW_STATUS_FILE"; then
    # 컨테이너 환경에서는 상태 파일로 대체 확인
    MODE=$(cat "$UFW_STATUS_FILE")
    echo "Firewall (UFW)... [OK] Active (${MODE})"
else
    # 경고만 출력하고 스크립트는 계속 진행 (exit 없음)
    echo "[WARNING] Firewall (UFW) is not active!"
fi

echo ""

# =============================================================
# [3] 자원 수집 — CPU, 메모리, 디스크 사용률 측정
# =============================================================
echo "[RESOURCE MONITORING]"

# CPU 사용률 수집
# top -bn1 : top 명령을 1번(n1)만 배치 모드(b)로 실행
# grep "Cpu(s)" : CPU 통계 줄만 추출
# awk '{print $2}' : 두 번째 컬럼(사용자 CPU%) 추출
# cut -d'%' -f1 : % 기호 앞의 숫자만 추출
CPU_USAGE=$(top -bn1 | grep "Cpu(s)" | awk '{print $2}' | cut -d'%' -f1)
# printf "%.1f" : 소수점 첫 번째 자리까지 포맷 (예: 5.3)
CPU_USAGE=$(printf "%.1f" "${CPU_USAGE:-0}")

# 메모리 사용률 수집
# free : 메모리 사용 현황 출력
# grep Mem : 물리적 메모리 줄만 추출
# awk '{print $2}' : 전체 메모리(total)
# awk '{print $3}' : 사용 중인 메모리(used)
MEM_TOTAL=$(free | grep Mem | awk '{print $2}')
MEM_USED=$(free | grep Mem | awk '{print $3}')
if [ "${MEM_TOTAL:-0}" -gt 0 ]; then
    # awk로 나눗셈 계산 (bash는 정수 계산만 지원하므로 awk 사용)
    MEM_USAGE=$(awk "BEGIN {printf \"%.1f\", ($MEM_USED / $MEM_TOTAL) * 100}")
else
    MEM_USAGE="0.0"
fi

# 디스크 사용률 수집
# df / : 루트(/) 파티션의 디스크 사용 현황
# tail -1 : 헤더를 제외한 데이터 줄만 (마지막 줄)
# awk '{print $5}' : 사용률 컬럼(예: 23%) 추출
# tr -d '%' : % 기호 제거하여 숫자만 남김
DISK_USAGE=$(df / | tail -1 | awk '{print $5}' | tr -d '%')

echo "CPU Usage  : ${CPU_USAGE}%"
echo "MEM Usage  : ${MEM_USAGE}%"
echo "DISK Used  : ${DISK_USAGE}%"
echo ""

# =============================================================
# [4] 임계값 경고 — 기준치 초과 시 [WARNING] 출력 (종료 안 함)
# 이유: 일시적 급증일 수 있고, 서비스는 정상 동작 중이기 때문
# =============================================================

# 소수점이 있으면 비교 오류가 나므로 정수 부분만 추출
CPU_INT=$(echo "$CPU_USAGE" | cut -d'.' -f1)
if [ "${CPU_INT:-0}" -gt "$CPU_THRESHOLD" ]; then
    echo "[WARNING] CPU threshold exceeded (${CPU_USAGE}% > ${CPU_THRESHOLD}%)"
fi

MEM_INT=$(echo "$MEM_USAGE" | cut -d'.' -f1)
if [ "${MEM_INT:-0}" -gt "$MEM_THRESHOLD" ]; then
    echo "[WARNING] MEM threshold exceeded (${MEM_USAGE}% > ${MEM_THRESHOLD}%)"
fi

if [ "${DISK_USAGE:-0}" -gt "$DISK_THRESHOLD" ]; then
    echo "[WARNING] DISK threshold exceeded (${DISK_USAGE}% > ${DISK_THRESHOLD}%)"
fi

echo ""
echo "===================================="

# =============================================================
# [5] 로그 기록 — 결과를 파일에 저장
# =============================================================

# 로그 디렉토리가 없으면 생성
LOG_DIR=$(dirname "$LOG_FILE")
if [ ! -d "$LOG_DIR" ]; then
    mkdir -p "$LOG_DIR" 2>/dev/null
fi

# 로그 로테이션 실행 (10MB 초과 시 자동 분리)
rotate_log

# 로그 한 줄 추가 기록
# >> 를 사용하여 누적 기록 (> 는 덮어쓰기이므로 절대 사용 금지!)
echo "[${TIMESTAMP}] PID:${PID} CPU:${CPU_USAGE}% MEM:${MEM_USAGE}% DISK_USED:${DISK_USAGE}%" >> "$LOG_FILE"

echo "[INFO] Log appended: $LOG_FILE"
```

저장: `Ctrl + O` → Enter → `Ctrl + X`

### 8.2 스크립트 소유자와 권한 설정

```bash
# 스크립트 소유자를 agent-dev, 그룹을 agent-core로 설정
# 왜 agent-dev가 소유자인가? → 개발자가 스크립트를 작성/관리하기 때문
# 왜 그룹이 agent-core인가? → agent-admin(cron 실행자)이 agent-core 소속이어야 실행 가능
sudo chown agent-dev:agent-core /home/agent-admin/agent-app/bin/monitor.sh

# 권한 750 설정
# 7(rwx) = agent-dev(소유자): 읽기+쓰기+실행 (스크립트 수정 가능)
# 5(r-x) = agent-core(그룹): 읽기+실행 (agent-admin이 cron으로 실행 가능)
# 0(---) = 기타: 완전 차단
sudo chmod 750 /home/agent-admin/agent-app/bin/monitor.sh

# 권한 확인
ls -la /home/agent-admin/agent-app/bin/monitor.sh
# 출력: -rwxr-x--- 1 agent-dev agent-core ... monitor.sh
```

### 8.3 스크립트 동작 테스트

앱이 실행 중인 상태에서 테스트합니다.

```bash
# agent-admin 계정으로 monitor.sh 실행
# sudo -u 계정명 : 해당 계정으로 명령 실행
sudo -u agent-admin /home/agent-admin/agent-app/bin/monitor.sh
```

**정상 출력 예시:**

```
====== SYSTEM MONITOR RESULT ======

[HEALTH CHECK]
Checking process 'agent-app-linux-arm64'... [OK] (PID: 1234)
Checking port 15034... [OK]

[FIREWALL CHECK]
Firewall (UFW)... [OK] Active

[RESOURCE MONITORING]
CPU Usage  : 2.3%
MEM Usage  : 4.2%
DISK Used  : 23%

====================================
[INFO] Log appended: /var/log/agent-app/monitor.log
```

```bash
# 로그 파일에 기록이 됐는지 확인
# tail -5 : 파일의 마지막 5줄만 출력
tail -5 /var/log/agent-app/monitor.log
# 출력: [2026-05-28 10:00:01] PID:1234 CPU:2.3% MEM:4.2% DISK_USED:23%
```

---

## 9단계: crontab 자동 실행 등록

### crontab이 무엇인가요?

crontab은 **"정해진 시간마다 자동으로 명령을 실행"** 해주는 스케줄러입니다.
`* * * * *`는 "매분마다"를 의미합니다.

```
# cron 시간 형식 설명
*  *  *  *  *   명령어
│  │  │  │  │
│  │  │  │  └── 요일 (0=일요일, 6=토요일)
│  │  │  └───── 월 (1~12)
│  │  └──────── 일 (1~31)
│  └─────────── 시 (0~23)
└────────────── 분 (0~59)

# 예시
* * * * * 명령어     → 매분 실행
0 * * * * 명령어     → 매시 정각 실행
0 2 * * * 명령어     → 매일 새벽 2시 실행
```

### 9.1 agent-admin의 crontab 등록

```bash
# agent-admin의 crontab을 편집
# crontab -e -u 계정명 : 해당 계정의 crontab 편집
sudo crontab -e -u agent-admin
```

편집기가 열리면 아래 줄을 추가합니다:

```
# 매분(* * * * *)마다 monitor.sh를 실행하고, 결과(표준출력+오류)를 로그에 추가 기록
* * * * * /home/agent-admin/agent-app/bin/monitor.sh >> /var/log/agent-app/monitor.log 2>&1
```

- `* * * * *` : 매분 실행
- `/home/agent-admin/agent-app/bin/monitor.sh` : 실행할 스크립트 (절대 경로 사용!)
- `>> /var/log/agent-app/monitor.log` : 표준 출력을 로그 파일에 추가
- `2>&1` : 오류 출력(stderr)도 표준 출력(stdout)과 같은 곳으로 보냄

저장 후 종료합니다.

### 9.2 crontab 등록 확인

```bash
# agent-admin의 crontab 내용 확인
sudo crontab -l -u agent-admin
# 위에서 추가한 줄이 보이면 정상
```

### 9.3 자동 실행 확인 (1~2분 대기)

```bash
# 현재 로그 파일의 줄 수 확인
wc -l /var/log/agent-app/monitor.log

# 1~2분 기다린 후 다시 확인 (줄 수가 늘어나면 자동 실행 중)
wc -l /var/log/agent-app/monitor.log

# 최근 3줄 확인 (매분 새 줄이 추가됩니다)
tail -3 /var/log/agent-app/monitor.log
```

**정상 출력 예시:**

```
[2026-05-28 10:01:01] PID:1234 CPU:2.3% MEM:4.2% DISK_USED:23%
[2026-05-28 10:02:01] PID:1234 CPU:1.8% MEM:4.2% DISK_USED:23%
[2026-05-28 10:03:01] PID:1234 CPU:2.1% MEM:4.3% DISK_USED:23%
```

매분 새 줄이 추가되면 자동 실행이 잘 되고 있는 것입니다.

---

## 10단계: 보너스 스크립트 (선택)

### 보너스 ① report.sh — 로그 통계 분석

monitor.log를 읽어서 CPU/메모리/디스크의 평균·최대·최소를 분석하는 스크립트입니다.

```bash
sudo nano /home/agent-admin/agent-app/bin/report.sh
```

```bash
#!/bin/bash
# =============================================================
# report.sh - monitor.log 통계 분석 리포트
# 사용법: ./report.sh [시작시간] [종료시간]
# 예시:   ./report.sh "2026-05-28 10:00:00" "2026-05-28 11:00:00"
# 인자가 없으면 전체 로그를 분석합니다
# =============================================================

LOG_FILE="/var/log/agent-app/monitor.log"
START_TIME="$1"   # 첫 번째 인자: 분석 시작 시간
END_TIME="$2"     # 두 번째 인자: 분석 종료 시간

# 로그 파일 존재 확인
if [ ! -f "$LOG_FILE" ]; then
    echo "[ERROR] 로그 파일 없음: $LOG_FILE"
    exit 1
fi

# 시간 범위가 지정된 경우 해당 구간만 추출
if [ -n "$START_TIME" ] && [ -n "$END_TIME" ]; then
    # awk로 시간 범위 필터링
    # $0 >= s && $0 <= e : 줄 전체(타임스탬프 포함)를 문자열로 비교
    LOG_DATA=$(awk -v s="[$START_TIME" -v e="[$END_TIME" '$0 >= s && $0 <= e' "$LOG_FILE")
    echo "분석 범위: $START_TIME ~ $END_TIME"
else
    # 인자 없으면 전체 로그 분석
    LOG_DATA=$(cat "$LOG_FILE")
    echo "분석 범위: 전체 로그"
fi

if [ -z "$LOG_DATA" ]; then
    echo "[WARNING] 분석할 데이터가 없습니다."
    exit 0
fi

# awk로 한 번에 전체 로그를 순회하면서 통계 계산
# 로그 형식: [2026-05-28 10:00:01] PID:1234 CPU:2.3% MEM:4.2% DISK_USED:23%
# 필드 번호: $1=[날짜  $2=시간]  $3=PID:N  $4=CPU:X%  $5=MEM:X%  $6=DISK_USED:X%
echo "$LOG_DATA" | awk '
NF >= 6 {
    # 타임스탬프 추출 ([ 와 ] 제거)
    ts = substr($1, 2) " " substr($2, 1, length($2)-1)

    # 각 필드에서 숫자만 추출 (split으로 : 기준 분리)
    split($4, ca, ":"); cpu = ca[2] + 0
    split($5, ma, ":"); mem = ma[2] + 0
    split($6, da, ":"); disk = da[2] + 0

    # 합계 누적 및 샘플 수 증가
    cpu_sum += cpu; mem_sum += mem; disk_sum += disk; count++

    # 최대/최소값 갱신 (첫 번째 데이터이거나 현재 값이 더 크면/작으면 업데이트)
    if (count == 1 || cpu  > cpu_max)  { cpu_max  = cpu;  cpu_max_ts  = ts }
    if (count == 1 || cpu  < cpu_min)  { cpu_min  = cpu;  cpu_min_ts  = ts }
    if (count == 1 || mem  > mem_max)  { mem_max  = mem;  mem_max_ts  = ts }
    if (count == 1 || mem  < mem_min)  { mem_min  = mem;  mem_min_ts  = ts }
    if (count == 1 || disk > disk_max) { disk_max = disk; disk_max_ts = ts }
    if (count == 1 || disk < disk_min) { disk_min = disk; disk_min_ts = ts }
}
END {
    if (count == 0) { print "[WARNING] 데이터 없음"; exit }
    printf "\n    ====== STATISTICS REPORT ======\n"
    printf "      [CPU]\n"
    printf "        Average : %.1f%%\n",        cpu_sum  / count
    printf "        Maximum : %.1f%% at %s\n",  cpu_max,  cpu_max_ts
    printf "        Minimum : %.1f%% at %s\n",  cpu_min,  cpu_min_ts
    printf "      [Memory]\n"
    printf "        Average : %.1f%%\n",        mem_sum  / count
    printf "        Maximum : %.1f%% at %s\n",  mem_max,  mem_max_ts
    printf "        Minimum : %.1f%% at %s\n",  mem_min,  mem_min_ts
    printf "      [Disk]\n"
    printf "        Average : %.1f%%\n",        disk_sum / count
    printf "        Maximum : %.1f%% at %s\n",  disk_max, disk_max_ts
    printf "        Minimum : %.1f%% at %s\n",  disk_min, disk_min_ts
    printf "      [Samples]\n"
    printf "        Data Points: %d samples\n\n", count
}
'
```

```bash
# report.sh 실행 권한 설정
sudo chmod +x /home/agent-admin/agent-app/bin/report.sh

# 사용 방법
sudo -u agent-admin /home/agent-admin/agent-app/bin/report.sh
# 또는 시간 범위 지정
sudo -u agent-admin /home/agent-admin/agent-app/bin/report.sh "2026-05-28 10:00:00" "2026-05-28 11:00:00"
```

---

### 보너스 ② log_archive.sh — 로그 자동 압축/삭제

7일이 지난 로그를 압축하고, 30일이 지난 압축 파일을 자동 삭제하는 스크립트입니다.

```bash
sudo nano /home/agent-admin/agent-app/bin/log_archive.sh
```

```bash
#!/bin/bash
# =============================================================
# log_archive.sh - 시간 기반 로그 보존 정책
# 왜 필요한가? 로그를 무한정 쌓아두면 디스크가 가득 차 서버 장애 발생!
# - 7일 경과 로그: 압축(.gz) 후 아카이브 폴더로 이동
# - 30일 경과 아카이브: 자동 삭제
# =============================================================

LOG_DIR="/var/log/agent-app"                       # 원본 로그 위치
ARCHIVE_DIR="/var/log/monitor/agent-app/archive"   # 압축 파일 보관 위치

# ---- 아카이브 디렉토리가 없으면 생성 ----
if [ ! -d "$ARCHIVE_DIR" ]; then
    mkdir -p "$ARCHIVE_DIR" 2>/dev/null
    if [ $? -ne 0 ]; then
        # $? : 직전 명령의 종료 코드 (0=성공, 1이상=실패)
        echo "[ERROR] 아카이브 디렉토리 생성 실패: $ARCHIVE_DIR (권한 부족)"
        exit 1
    fi
    echo "[INFO] 아카이브 디렉토리 생성: $ARCHIVE_DIR"
fi

# ---- 로그 디렉토리 존재 확인 ----
if [ ! -d "$LOG_DIR" ]; then
    echo "[ERROR] 로그 디렉토리 없음: $LOG_DIR"
    exit 1
fi

# ---- 7일 이상 된 로그 파일 압축 → 아카이브로 이동 ----
echo "[INFO] 7일 경과 로그 파일 압축 중..."

# find : 조건에 맞는 파일 검색
# -name "*.log" : 확장자가 .log 인 파일
# -mtime +7 : 수정된 지 7일 초과된 파일 (+7 = 7일 초과)
OLD_FILES=$(find "$LOG_DIR" -name "*.log" -mtime +7 2>/dev/null)

if [ -z "$OLD_FILES" ]; then
    echo "[INFO] 압축 대상 없음 (7일 이내 로그만 존재)"
else
    # 발견된 각 파일에 대해 반복 처리
    echo "$OLD_FILES" | while IFS= read -r file; do
        BASENAME=$(basename "$file")   # 경로에서 파일명만 추출
        # 압축 파일 이름: 원본명.날짜.gz (예: monitor.log.20260521.gz)
        DEST="${ARCHIVE_DIR}/${BASENAME}.$(date +%Y%m%d).gz"

        # gzip -c : 압축 결과를 파일에 쓰기 (-c = stdout으로 출력)
        if gzip -c "$file" > "$DEST" 2>/dev/null; then
            rm -f "$file"   # 압축 성공하면 원본 삭제
            echo "[OK] 압축/이동: $file → $DEST"
        else
            echo "[WARNING] 압축 실패: $file"
        fi
    done
fi

# ---- 30일 이상 된 압축 파일 삭제 ----
echo "[INFO] 30일 경과 아카이브 파일 삭제 중..."
OLD_ARCHIVES=$(find "$ARCHIVE_DIR" -name "*.gz" -mtime +30 2>/dev/null)

if [ -z "$OLD_ARCHIVES" ]; then
    echo "[INFO] 삭제 대상 없음 (30일 이내 아카이브만 존재)"
else
    echo "$OLD_ARCHIVES" | while IFS= read -r archive; do
        rm -f "$archive"
        echo "[OK] 삭제: $archive"
    done
fi

echo "[INFO] 로그 보존 정책 완료"
```

```bash
# 실행 권한 설정
sudo chmod +x /home/agent-admin/agent-app/bin/log_archive.sh

# 실행 테스트
sudo /home/agent-admin/agent-app/bin/log_archive.sh
```

---

## 최종 확인 체크리스트

```bash
# ✅ 1. SSH 포트 확인
sudo grep -E "^Port|^PermitRootLogin" /etc/ssh/sshd_config
# Port 20022 / PermitRootLogin no

# ✅ 2. 방화벽 상태 확인
sudo ufw status
# Status: active / 20022, 15034 ALLOW

# ✅ 3. 계정 그룹 확인
id agent-admin   # groups에 agent-common, agent-core 포함
id agent-dev     # groups에 agent-common, agent-core 포함
id agent-test    # groups에 agent-common만 포함

# ✅ 4. 앱 실행 (Boot Sequence)
sudo -u agent-admin bash -c 'source /home/agent-admin/.bashrc && /home/agent-admin/agent-app/agent-app-linux-arm64'
# Agent READY 출력 확인

# ✅ 5. monitor.sh 실행 확인
sudo -u agent-admin /home/agent-admin/agent-app/bin/monitor.sh
# [OK] 메시지 확인

# ✅ 6. 로그 누적 확인
tail -5 /var/log/agent-app/monitor.log
# 최근 5줄 표시

# ✅ 7. crontab 매분 자동 실행 확인
sudo crontab -l -u agent-admin
# * * * * * ... monitor.sh ... 라인 확인

# ✅ 8. 1분 후 로그 자동 증가 확인
wc -l /var/log/agent-app/monitor.log
# (1분 기다린 후 다시 실행하면 줄 수가 늘어납니다)
```

---

## 평가 항목별 설명 가이드

평가자 앞에서 각 항목을 설명하는 방법입니다.

---

### 항목 1: 구현 결과 확인

**1-1. SSH 포트 변경 + Root 차단**

```bash
# 평가자 앞에서 실행할 확인 명령어
sudo grep -E "^Port|^PermitRootLogin" /etc/ssh/sshd_config
ss -tlnp | grep 20022
```

설명:

> "SSH 기본 포트 22를 20022로 바꾸고, root 원격 로그인을 차단했습니다.
> 22번 포트는 자동화된 공격이 집중되므로 포트 변경만으로 공격 노이즈가 크게 줄어듭니다."

---

**1-2. 방화벽(UFW)**

```bash
sudo ufw status
```

설명:

> "기본 정책은 모든 외부 접속 차단(deny)이고, SSH(20022)와 앱 포트(15034)만 명시적으로 허용했습니다."

---

**1-3. 계정/그룹 구성**

```bash
id agent-admin
id agent-dev
id agent-test
```

설명:

> "업무 역할에 따라 3개 계정과 2개 그룹을 만들었습니다.
> agent-test는 agent-core에 속하지 않아 API 키와 운영 로그에 접근할 수 없습니다.
> 이것이 최소 권한 원칙입니다."

---

**1-4~1-8. monitor.sh와 crontab**

```bash
sudo -u agent-admin /home/agent-admin/agent-app/bin/monitor.sh
tail -5 /var/log/agent-app/monitor.log
sudo crontab -l -u agent-admin
```

설명:

> "monitor.sh는 프로세스와 포트가 비정상이면 즉시 exit 1로 종료하고,
> 방화벽이나 임계값 초과는 경고만 하고 계속 실행합니다.
> 서비스 중단 상황은 즉시 알려야 하고, 임시 급증은 로그로 추적하면 되기 때문입니다."

---

### 항목 2: 구현 방법 설명

**프로세스 확인에 `ps + grep`을 쓴 이유:**

```bash
# ps -eo pid,args : 전체 명령 경로까지 포함해 검색 (pgrep -f 와 동일 효과)
# grep -v grep : grep 자신이 검색 결과에 잡히는 것 방지
PID=$(ps -eo pid,args | grep "$APP_PROCESS" | grep -v grep | awk '{print $1}' | head -1)
```

> "`pgrep -f`로도 같은 결과를 얻을 수 있습니다. `ps`를 쓴 이유는 전체 경로를 기준으로 검색하면
> 비슷한 이름의 다른 프로세스와 혼동을 막을 수 있기 때문입니다."

---

**CPU/MEM/DISK 추출 방법:**

```bash
# CPU: top을 1회만 실행(-bn1)하여 Cpu(s) 줄에서 숫자 추출
CPU_USAGE=$(top -bn1 | grep "Cpu(s)" | awk '{print $2}' | cut -d'%' -f1)

# MEM: free 명령의 total/used를 가져와 직접 계산
MEM_USAGE=$(awk "BEGIN {printf \"%.1f\", ($MEM_USED / $MEM_TOTAL) * 100}")

# DISK: df /의 5번째 컬럼(사용률)에서 % 제거
DISK_USAGE=$(df / | tail -1 | awk '{print $5}' | tr -d '%')
```

> "로그 포맷을 `키:값%` 형식으로 고정한 이유는 `report.sh`에서 `awk`로
> `split($4, ca, ":")`처럼 파싱하기 쉽게 하기 위해서입니다."

---

**권한 정책 구현:**

```bash
# monitor.sh: 소유자 agent-dev, 그룹 agent-core, 권한 750
chown agent-dev:agent-core monitor.sh
chmod 750 monitor.sh
```

> "agent-admin은 agent-core 그룹에 속하므로 그룹 권한(r-x=5)으로 실행 가능합니다.
> agent-test는 agent-core에 없으므로 기타 권한(---=0)이 적용되어 실행 불가입니다."

---

**로그 용량 관리:**

> "logrotate 대신 스크립트에 직접 `rotate_log()` 함수를 구현했습니다.
> 외부 도구 의존 없이 monitor.sh 하나로 완결되고, 10MB/10개 조건을 코드로 명확히 표현할 수 있기 때문입니다."

---

### 항목 3: 보안 설계 이유

**SSH 포트 변경의 위협 모델:**

> "인터넷에 연결된 서버는 매일 수천 번의 자동화된 공격(Brute Force)을 받습니다.
> 공격 봇은 기본 포트 22를 스캔하도록 설계되어 있어, 포트를 바꾸면 대부분의 공격이 우리 서버를 지나칩니다.
> 뚫리더라도 root 차단이 있으니 두 겹의 방어입니다."

---

**최소 권한 원칙 (api_keys + 로그 디렉토리):**

> "api_keys와 /var/log/agent-app은 agent-core만 접근 가능합니다.
> agent-test는 테스트 계정이므로 API 키나 운영 로그를 볼 이유가 없습니다.
> 실수로 키를 삭제하거나 외부에 노출되는 피해 범위를 최소화합니다."

---

**경고만 출력하는 항목 vs 종료(exit 1)하는 항목:**

| 상황                     | 처리 방법      | 이유                               |
| ------------------------ | -------------- | ---------------------------------- |
| 프로세스 다운            | exit 1 종료    | 서비스 완전 중단 — 즉시 알려야 함 |
| 포트 닫힘                | exit 1 종료    | 서비스 접속 불가 — 즉시 알려야 함 |
| 방화벽 비활성            | [WARNING] 출력 | 보안 위험이지만 서비스는 정상      |
| CPU/MEM/DISK 임계치 초과 | [WARNING] 출력 | 일시적 급증일 수 있음              |

> "exit 1로 종료하면 cron이 '실패'로 기록하고 이후 로그도 안 쌓입니다.
> 경고만 하면 계속 모니터링하면서 데이터를 누적할 수 있어 트렌드 분석이 가능합니다."

---

**`>` vs `>>`의 차이:**

> "`>`는 덮어쓰기여서 매분 로그가 1줄로 초기화됩니다.
> `>>`는 이어쓰기여서 매분 1줄씩 누적됩니다. 로그는 반드시 `>>`를 써야 합니다."

---

### 항목 4: 응용/장애 대응

**모니터링 대상이 Nginx로 바뀐다면?**

```bash
# monitor.sh에서 이 두 줄만 바꾸면 됩니다
APP_PROCESS="nginx"      # agent-app-linux-arm64 → nginx
APP_PORT=80              # 15034 → 80 (또는 443)
# 로그 경로도 /var/log/nginx/ 로 변경 권장
```

---

**"프로세스는 살아있는데 포트가 안 열리는 상황":**

```
1. ss -tlnp | grep 15034    → 포트 바인딩 실패 확인
2. tail -50 /var/log/agent-app/monitor.log → 앱 오류 메시지 확인
3. ss -tlnp | grep 15034    → 다른 프로세스가 포트 점유 중인지 확인
4. sudo ufw status           → 방화벽이 포트를 막고 있는지 확인
5. 앱 프로세스 kill 후 재시작
```

---

**로그가 급증해 디스크가 가득 찰 위험:**

단기 대응 (지금 당장):

```bash
df -h                              # 디스크 현황 파악
du -sh /var/log/agent-app/*        # 큰 파일 찾기
gzip /var/log/agent-app/monitor.log.5  # 오래된 로그 즉시 압축
rm /var/log/agent-app/monitor.log.9   # 필요 시 삭제
```

중기 대응 (재발 방지):

- `MAX_LOG_SIZE`를 더 작게 (10MB → 5MB)
- `MAX_LOG_FILES`를 줄임 (10개 → 5개)
- `log_archive.sh`를 crontab에 등록 (매일 새벽 실행)
- 디스크 경고 임계값 낮추기 (80% → 70%)

---

## 자주 묻는 질문 (FAQ)

**Q. `sudo`를 쓰는데 비밀번호를 묻지 않으면?**
A. sudoers에 NOPASSWD가 설정된 경우입니다. agent-admin이 그렇게 설정되어 있습니다.

**Q. `command not found` 오류가 나면?**
A. 해당 도구가 설치되지 않은 것입니다. `sudo apt install -y 도구명` 으로 설치하세요.

**Q. 파일 편집 후 저장이 안 될 때?**
A. nano에서 `Ctrl + O` → Enter → `Ctrl + X` 순서로 저장합니다. 권한 오류라면 `sudo nano`로 다시 열어야 합니다.

**Q. cron이 실행되는데 로그가 안 쌓일 때?**
A. 앱이 실행 중인지 먼저 확인하세요. monitor.sh는 프로세스가 없으면 exit 1로 종료하므로 로그가 안 쌓입니다.

**Q. 포트를 바꿨는데 SSH 접속이 끊겼을 때?**
A. OrbStack 환경에서는 `orb shell b1-lab`으로 접속하면 됩니다. `orb shell`은 SSH와 별개의 방법을 사용하므로 SSH 포트 변경의 영향을 받지 않습니다.

---

## 핵심 명령어 빠른 참조표

| 목적                | 명령어                                  |
| ------------------- | --------------------------------------- |
| OS 버전 확인        | `cat /etc/os-release`                 |
| 현재 사용자 확인    | `whoami`                              |
| 패키지 설치         | `sudo apt install -y 패키지명`        |
| SSH 서비스 재시작   | `sudo systemctl restart sshd`         |
| 방화벽 포트 허용    | `sudo ufw allow 포트/tcp`             |
| 방화벽 활성화       | `sudo ufw enable`                     |
| 방화벽 상태 확인    | `sudo ufw status`                     |
| 그룹 생성           | `sudo groupadd 그룹명`                |
| 계정 생성           | `sudo useradd -m -s /bin/bash 계정명` |
| 비밀번호 설정       | `sudo passwd 계정명`                  |
| 그룹에 계정 추가    | `sudo usermod -aG 그룹명 계정명`      |
| 계정 그룹 확인      | `id 계정명`                           |
| 폴더 생성           | `sudo mkdir -p 경로`                  |
| 소유자 변경         | `sudo chown 소유자:그룹 경로`         |
| 권한 변경           | `sudo chmod 숫자 경로`                |
| ACL 설정            | `sudo setfacl -m g:그룹:rwx 경로`     |
| ACL 확인            | `getfacl 경로`                        |
| 계정 전환           | `su - 계정명`                         |
| 환경 변수 즉시 적용 | `source ~/.bashrc`                    |
| 포트 리슨 상태 확인 | `ss -tulnp \| grep 포트`               |
| 로그 파일 끝 확인   | `tail -5 파일경로`                    |
| 파일 줄 수 확인     | `wc -l 파일경로`                      |
| crontab 편집        | `sudo crontab -e -u 계정명`           |
| crontab 확인        | `sudo crontab -l -u 계정명`           |

---

> 이 교본은 실제 수행한 내용을 바탕으로 작성되었습니다.
> 막히는 부분이 있으면 각 단계의 **"확인" 명령어** 부터 다시 실행해보세요.

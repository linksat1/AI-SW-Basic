# 작업기록 (2026-05-29)

## 환경: OrbStack Linux VM (b1-lab-test, x86_64)

---

```
# Mac 터미널에서 VM 접속
$ orb shell -m b1-lab-test

# 패키지 설치
$ sudo apt install -y acl ufw net-tools unzip

# SSH 서버 시작
$ sudo systemctl start ssh

# SSH 설정 확인
$ sudo grep -E "^Port|^PermitRootLogin" /etc/ssh/sshd_config
Port 20022
PermitRootLogin no

# SSH 서버 포트 확인
$ sudo ss -tulnp | grep sshd

# 방화벽 상태 확인
$ sudo ufw status
To                         Action      From
--                         ------      ----
20022/tcp                  ALLOW       Anywhere
15034/tcp                  ALLOW       Anywhere
20022/tcp (v6)             ALLOW       Anywhere (v6)
15034/tcp (v6)             ALLOW       Anywhere (v6)

# 사용자 생성
$ sudo useradd -m -s /bin/bash agent-admin

# 그룹 추가
$ sudo usermod -aG agent-common agent-admin

# sudo 권한 설정
$ sudo visudo
# 추가 내용: agent-admin ALL=(ALL) NOPASSWD:ALL

# 사용자 확인
$ id agent-admin
uid=1000(agent-admin) gid=1002(agent-admin) groups=1002(agent-admin),1000(agent-common),1001(agent-core)

# agent-admin 계정으로 전환
$ su - agent-admin

# agent-app 디렉토리 생성
$ mkdir ~/agent-app

# zip 파일 복사
$ cp /Users/cspag5955/cspag/AI-SW-Basic/agent-app.zip /tmp/

# 압축 해제
$ unzip /tmp/agent-app.zip -d ~/agent-app

# 파일 목록 확인
$ ls ~/agent-app/
agent-app-linux-arm64  agent-app-linux-x86  api_keys  bin  __MACOSX  upload_files

# secret.key 파일명 변경
$ mv ~/agent-app/api_keys/t_secret.key ~/agent-app/api_keys/secret.key

# 로그 디렉토리 생성
$ sudo mkdir -p /var/log/agent-app

# 소유자 설정
$ sudo chown -R agent-admin:agent-admin /home/agent-admin/agent-app

# secret.key 권한 설정
$ chmod 600 /home/agent-admin/agent-app/api_keys/secret.key

# 환경변수 설정
$ cat >> /home/agent-admin/.bashrc << 'EOF'
export AGENT_HOME=/home/agent-admin/agent-app
export AGENT_PORT=15034
export AGENT_UPLOAD_DIR=$AGENT_HOME/upload_files
export AGENT_KEY_PATH=$AGENT_HOME/api_keys
export AGENT_LOG_DIR=/var/log/agent-app
EOF

$ source ~/.bashrc

# 실행 권한 부여
$ chmod +x ~/agent-app/agent-app-linux-x86

# agent-app 백그라운드 실행
$ ./agent-app/agent-app-linux-x86 &
>>> Starting Agent Boot Sequence...
[1/5] Checking User Account               [OK]
   ... Running as service user 'agent-admin' (uid=1000)
[2/5] Verifying Environment Variables     [OK]
   ... All required Envs correct
[3/5] Checking Required Files             [OK]
   ... Verified 'secret.key' with correct key string.
[4/5] Checking Port Availability          [OK]
[5/5] Verifying Log Permission            [OK]

# monitor.sh APP_PROCESS 수정 (arm64 → x86)
$ sed -i 's/APP_PROCESS="agent-app-linux-arm64"/APP_PROCESS="agent-app-linux-x86"/' \
    /home/agent-admin/agent-app/bin/monitor.sh

# monitor.sh 소유자 및 권한 설정
$ sudo chown agent-dev:agent-core /home/agent-admin/agent-app/bin/monitor.sh
$ sudo chmod 750 /home/agent-admin/agent-app/bin/monitor.sh

# monitor.sh 수동 실행 테스트
$ sudo -u agent-admin /home/agent-admin/agent-app/bin/monitor.sh
====== SYSTEM MONITOR RESULT ======

[HEALTH CHECK]
Checking process 'agent-app-linux-x86'... [OK] (PID: 2064)
Checking port 15034... [OK]

[FIREWALL CHECK]
Firewall (UFW)... [OK] Active

[RESOURCE MONITORING]
CPU Usage  : 0.0%
MEM Usage  : 4.7%
DISK Used  : 2%

====================================

# crontab 등록 (매분 자동 실행)
$ crontab -e
# 추가 내용:
* * * * * /home/agent-admin/agent-app/bin/monitor.sh >> /var/log/agent-app/monitor.log 2>&1

# cron 서비스 확인
$ sudo systemctl status cron
● cron.service - Regular background program processing daemon
     Active: active (running)

# 로그 확인
$ tail -3 /var/log/agent-app/monitor.log
====================================
[2026-05-29 21:23:01] PID:2064 CPU:0.0% MEM:4.4% DISK_USED:2%
[INFO] Log appended: /var/log/agent-app/monitor.log
```

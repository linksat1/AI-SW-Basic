# B6-1 트러블슈팅 보고서 [템플릿 — 실습 후 반드시 실제 내용으로 교체]

> ⚠️ 이 파일은 템플릿입니다. AWS 콘솔/CLI 작업 중 **실제로 겪은 문제**를 최소 1건 이상
> 아래 6단계 구조(증상 → 원인 가설 → 검증 방법 → 조치 내용 → 결과 → 재발 방지)로 기록해야
> 제출 요건을 만족합니다. 문제를 겪지 않았다면, `가이드.md` 7장의 "자주 발생하는 문제" 표를
> 참고해 하나를 의도적으로 재현(예: SSH 소스를 일부러 잘못된 IP로 설정)한 뒤 해결 과정을
> 기록해도 됩니다 — 실제로 겪고 해결하는 경험 자체가 이 과제의 학습 목표입니다.

---

## 사건 #1 — <여기에 문제를 한 줄로 요약: 예) 배포 직후 외부 브라우저에서 접속 안 됨>

| 항목 | 내용 |
|---|---|
| 발생 일시 | \<YYYY-MM-DD HH:MM\> |
| 증상(문제 상황) | \<무엇을 시도했고 무엇이 예상과 다르게 동작했는지 구체적으로. 예: `http://<퍼블릭IP>` 접속 시 브라우저가 계속 로딩만 하다가 타임아웃됨\> |
| 원인 가설 | \<왜 그럴 것 같은지 1~2개 후보. 예: "Security Group 인바운드에 80번 포트가 열려있지 않을 것이다" / "Route Table에 0.0.0.0/0 경로가 없을 것이다"\> |
| 검증 방법 | \<가설을 어떻게 확인했는지. 예: EC2 콘솔 → 보안 → 보안 그룹 → 인바운드 규칙 확인 / VPC 콘솔 → 라우트 테이블 → 라우트 탭 확인 / 인스턴스 내부에서 `sudo systemctl status nginx`, `curl -i http://localhost` 실행\> |
| 조치 내용 | \<실제로 무엇을 바꿨는지. 예: 보안 그룹 인바운드에 "HTTP, TCP, 80, 0.0.0.0/0" 규칙 추가\> |
| 결과 | \<조치 후 재확인한 결과. 예: 브라우저 재접속 시 "Welcome to nginx!" 정상 표시, `curl -i http://<퍼블릭IP>` → `HTTP/1.1 200 OK`\> |
| 재발 방지 | \<다음에 같은 실수를 안 하려면 무엇을 체크리스트에 추가할지. 예: "EC2 배포 직후 SG 인바운드 80/22 규칙을 항상 콘솔에서 육안으로 재확인한다"\> |

**근거 스크린샷/로그**: `<docs/screenshots/troubleshooting-1-before.png>`,
`<docs/screenshots/troubleshooting-1-after.png>` (있다면 경로 기재)

---

## (선택) 사건 #2 — <추가로 겪은 문제가 있다면>

| 항목 | 내용 |
|---|---|
| 증상(문제 상황) | \<작성\> |
| 원인 가설 | \<작성\> |
| 검증 방법 | \<작성\> |
| 조치 내용 | \<작성\> |
| 결과 | \<작성\> |
| 재발 방지 | \<작성\> |

---

## 참고 — 자주 발생하는 문제 유형 (겪었을 때 대조용, 예시로만 사용할 것)

이 표는 실제 겪은 경험을 대체하지 않습니다. 위 사건 #1에는 반드시 **본인이 직접 확인한
증상과 조치**를 적어야 합니다.

| 증상 | 확인해볼 것 |
|---|---|
| 브라우저/curl에서 응답 없음(타임아웃) | SG 인바운드 80/TCP/0.0.0.0/0 존재 여부, Route Table의 0.0.0.0/0→IGW 존재 여부, IGW가 VPC에 Attach 됐는지 |
| Connection refused | 인스턴스 내부 `systemctl status nginx`로 실행 여부 확인 |
| SSH 접속 timeout | SG 인바운드 22/TCP의 소스 IP가 현재 본인 공인 IP와 일치하는지(`curl -s https://checkip.amazonaws.com`로 재확인 — IP는 네트워크 환경에 따라 바뀔 수 있음) |
| SSH Permission denied (publickey) | `.pem` 권한(`chmod 400`), 사용자명(Ubuntu=`ubuntu`, Amazon Linux=`ec2-user`) |
| /health가 404 | User data 스크립트 실행 여부(`cat /var/log/cloud-init-output.log`), `/var/www/html/health` 파일 존재 여부 |
| 인스턴스 내부에서 `curl https://example.com` 실패 | Route Table의 0.0.0.0/0→IGW 누락, 퍼블릭 IP 자동 할당 미설정 |
